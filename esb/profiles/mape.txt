# MAPE grid-search profile — full grid (approved Stage C search space, design §9).
# Launch: python -m esb run --profile mape
# NOTE: this is the UPDATED search space (neurons dropped 100; added a dropout
# axis with activation-aware layers), NOT the legacy grid. Scaling stays OFF here
# (see mape_scaled.txt for the --scale variant).
--loss mape
--lead_times 12 48 96 120
--rotations 0 1 2 3
--activations relu leaky_relu selu sigmoid tanh
--num_layers 1 2 3
--neurons 16 32 64 128 256
--dropout 0.0 0.05 0.1 0.3
--epochs 200000
--workers 4
--repetitions 1
--output_dir results/esb_tuner
